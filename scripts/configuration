# Customizable
HOTSPOT="ACCEPT" # or REJECT
BIN_NAME="meta"

# Advanced
DIR="${0%/*}"
MODDIR="/data/adb/modules/MODID"
DATA="/data/adb/MODID"
BASE="$DATA/base.yaml"
CONFIG="$DATA/config.yaml"
TEMPORARY="$DATA/.temporary.yaml"

# Logs
RUN="$MODDIR/run"
PID_FILE="$RUN/clash.pid"
RUN_LOG="$RUN/run.log"
CORE_LOG_FILE="$RUN/core.log"

# Variables
SCRIPTS="$MODDIR/scripts"
BIN_DIR="$MODDIR/bin"
BIN="$BIN_DIR/$BIN_NAME"
BUSYBOX="/data/adb/magisk/busybox"
